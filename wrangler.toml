name = "cloud-mail-frontend" # 可以自定义，仅用于标识
main = "" # Pages 部署，此项留空
compatibility_date = "2024-12-01"

[pages_build_output_dir]
# 👇 这是最关键的设置：指向 Vue 项目构建后的输出目录
dir = "mail-vue/dist"
# 👇 如果你知道你的前端路由是单页应用（SPA），建议启用此回退
fallback = "index.html"

[build]
# 👇 构建命令：进入前端目录，安装依赖并构建
command = "cd mail-vue && npm install && npm run build"
